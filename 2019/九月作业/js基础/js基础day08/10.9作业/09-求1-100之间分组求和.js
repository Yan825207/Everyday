//把1 - 100之间的数字，10个数字为一组，在控制台打印出每组的和，
//例如，1 - 10的和为一组，输出来，11 - 20的和为一组输出来，依次类推
for (var j = 1; j <= 10; j++) {
    var sum = 0;
    for (var i = (j - 1) * 10 + 1; i <= 10 * j; i++) {
        sum += i;
    }
    console.log(sum);
}
