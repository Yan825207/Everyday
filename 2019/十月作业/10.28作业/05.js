// 45.Math对象常用的一些方法有什么？
// 答：Math.random()，该方法返回大于等于0小于1的一个随机数，
// Math.ceil(),执行向上取整运算
// Math.floor() ,执行向下取整运算 
// Math.round(),执行四舍五入取整运算
// Math.max(),返回参数中最大值。如果没有参数则返回-Infinity。如果任意一个参数是NaN或不可转换为数字，则返回NaN
// Math.min(),　返回参数中最小值。如果没有参数则返回Infinity。如果任意一个参数是NaN或不可转换为数字，则返回NaN
// var a = Math.random(); //随机返回大于0小于1的数字
// Math.ceil(); //向上取整
// Math.floor();//向下取整
// Math.round();//四舍五入
// Math.max;//取最大值
// Math.min;//取最小值
// console.log(a)
// var date = new Date();
// getFullYear() //返回当前日期的年
// console.log(date.getFullYear()); 
// getMonth() //返回当前日期的月份，返回的月份小1个月 ，所以要月份+1 
// console.log(date.getMonth() + 1); 
// getDate() //返回的是几号
// console.log(date.getDate()); 
// getDay() //周一返回的是 1 ，周六返回的是 6 ，但是周日返回的是 0
// console.log(date.getDay()); 
