// 5.求1到100所有不能被3整除的整数的第一个大于2000的和
// 定义变量接收和
var num = 0
// 循环1-100
for (var i = 1; i <= 100; i++) {
    // 判断除以3不等于0则累加
    if (i % 3 != 0) {
        num += i
    }
    if (num >= 2000) {
        break
    }
    // 判断和大于2000则打印并跳出
}
console.log(num)